{% extends '@admin/_layout.twig' %}
{% block title %}{{ t('rev.title') }} — {{ page.title }}{% endblock %}

{% block content %}
  <h1>{{ t('rev.title') }}</h1>
  <p class="sub">
    <a href="?action=edit&amp;page={{ page.id }}">← {{ page.title }}</a> ·
    <code>{{ page.slug }}</code>
  </p>

  <div class="card">
    <table>
      <tr><th>{{ t('rev.when') }}</th><th></th></tr>
      {# Dates only. The contents of a revision are never put on screen — you do
         not need to read a version to choose it, and not printing it is one
         fewer place old, differently-sanitised HTML could surface. #}
      {% for rev in revisions %}
        <tr>
          <td>{{ rev.at }}</td>
          <td style="text-align:end">
            <form method="post" onsubmit="return confirm('{{ t('rev.confirm')|e('js') }}')">
              <input type="hidden" name="action" value="restore">
              <input type="hidden" name="csrf" value="{{ csrf }}">
              <input type="hidden" name="page" value="{{ page.id }}">
              <input type="hidden" name="revision" value="{{ rev.file }}">
              <button class="btn" type="submit">{{ t('rev.restore') }}</button>
            </form>
          </td>
        </tr>
      {% else %}
        <tr><td colspan="2">{{ t('rev.empty') }}</td></tr>
      {% endfor %}
    </table>
  </div>

  <p class="hint">
    {{ t('rev.intro') }}
  </p>
{% endblock %}
