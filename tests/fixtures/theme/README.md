# Frozen snapshot of the demo theme's components

Taken from panfotis/flatcms-theme-demo @ 81fd562. **Deliberately not synced** —
these are inputs to the test suite, and 1,100+ assertions are pinned to their
exact schemas. The suite needs them to hold still the way it needs
hostile_save.php to hold still; the demo theme is free to evolve without
moving what a lockdown case means.

Re-snapshot only on purpose, expecting to re-verify the whole suite.
