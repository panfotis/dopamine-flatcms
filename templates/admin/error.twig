{% extends 'admin/_layout.twig' %}
{% block title %}{{ t('err.title') }}{% endblock %}

{% block content %}
  <h1>{{ t('err.title') }}</h1>
  <div class="flash err">{{ message }}</div>
  <p><a class="btn ghost" href="?">{{ t('err.back') }}</a></p>
{% endblock %}
